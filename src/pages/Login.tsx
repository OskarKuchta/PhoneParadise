import { FC } from "react";

const Login: FC = () => {
  return (
    <main>
      <h2 className="m-8 flex flex-col justify-center items-center mx-[10%] text-center">
        login page
      </h2>
    </main>
  );
};

export default Login;
