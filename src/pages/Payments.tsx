import { FC } from "react";
import PaymentStart from "../components/Payment/PaymentStart";

const Payments: FC = () => {
  return (
    <section>
      <PaymentStart />
    </section>
  );
};

export default Payments;
